--- src/services/employee/profile.service.ts
+++ src/services/employee/profile.service.ts
@@ -5,12 +5,15 @@
   async getEmployeeProfile() {
     const { data: authData, error: authError } = await supabase.auth.getUser();
     if (authError || !authData.user) {
-      throw new Error('Not authenticated');
+      throw new Error('Authentication required');
     }
 
     const { data, error } = await supabase
       .from('employee_profiles')
       .select(`
         *,
         departments ( name ),
         designations ( name ),
         work_locations ( name ),
         reporting_manager:employee_profiles!reporting_manager_id ( full_name )
       `)
       .eq('user_id', authData.user.id)
       .single();
 
     if (error) {
-      throw error;
+      if (error.code === 'PGRST116') {
+        throw new Error('Employee profile is not linked to this account');
+      }
+      console.error('Supabase error fetching profile:', error);
+      throw new Error('Unable to load profile. Please try again.');
     }
     return data;
   },
 
   async updatePermittedFields(fields: {
     profile_photo_url?: string;
     mobile?: string;
     personal_email?: string;
     address?: unknown;
     emergency_contact_name?: string;
     emergency_contact_relationship?: string;
     emergency_contact_phone?: string;
   }) {
     const { data: authData, error: authError } = await supabase.auth.getUser();
     if (authError || !authData.user) {
-      throw new Error('Not authenticated');
+      throw new Error('Authentication required');
     }
 
     const { data: profile, error: profileError } = await supabase
       .from('employee_profiles')
       .select('id')
       .eq('user_id', authData.user.id)
       .single();
 
     if (profileError || !profile) {
-      throw new Error('Profile not found');
+      if (profileError?.code === 'PGRST116') {
+        throw new Error('Employee profile is not linked to this account');
+      }
+      throw new Error('Unable to load profile. Please try again.');
     }
 
     const { data, error } = await supabase
       .from('employee_profiles')
       .update(fields)
       .eq('id', profile.id)
       .select()
       .single();
 
     if (error) {
-      throw error;
+      console.error('Supabase error updating profile:', error);
+      throw new Error('Unable to update profile. Please try again.');
     }
     return data;
   },
 
   async completeProfile(fields: {
     mobile?: string;
     date_of_birth?: string;
     address?: unknown;
     emergency_contact_name?: string;
     emergency_contact_relationship?: string;
     emergency_contact_phone?: string;
   }) {
     const { data: authData, error: authError } = await supabase.auth.getUser();
     if (authError || !authData.user) {
-      throw new Error('Not authenticated');
+      throw new Error('Authentication required');
     }
 
     const { data: profile, error: profileError } = await supabase
       .from('employee_profiles')
       .select('id')
       .eq('user_id', authData.user.id)
       .single();
 
     if (profileError || !profile) {
-      throw new Error('Profile not found');
+      if (profileError?.code === 'PGRST116') {
+        throw new Error('Employee profile is not linked to this account');
+      }
+      throw new Error('Unable to load profile. Please try again.');
     }
 
     const { data, error } = await supabase
       .from('employee_profiles')
       .update({
         ...fields,
         profile_completion_status: 'completed'
       })
       .eq('id', profile.id)
       .select()
       .single();
 
     if (error) {
-      throw error;
+      console.error('Supabase error completing profile:', error);
+      throw new Error('Unable to update profile. Please try again.');
     }
     return data;
   },
 
   async requestChange(requestData: {
     field_name: string;
     current_value: unknown;
     requested_value: unknown;
     reason?: string;
     supporting_document_path?: string;
   }) {
     const { data: authData, error: authError } = await supabase.auth.getUser();
     if (authError || !authData.user) {
-      throw new Error('Not authenticated');
+      throw new Error('Authentication required');
     }
 
     const { data: profile, error: profileError } = await supabase
       .from('employee_profiles')
       .select('id')
       .eq('user_id', authData.user.id)
       .single();
 
     if (profileError || !profile) {
-      throw new Error('Profile not found');
+      if (profileError?.code === 'PGRST116') {
+        throw new Error('Employee profile is not linked to this account');
+      }
+      throw new Error('Unable to load profile. Please try again.');
     }
 
     const { data, error } = await supabase
       .from('profile_change_requests')
       .insert({
         employee_id: profile.id,
         ...requestData,
         status: 'submitted'
       })
       .select()
       .single();
 
     if (error) {
-      throw error;
+      console.error('Supabase error requesting change:', error);
+      throw new Error('Unable to request change. Please try again.');
     }
     return data;
   },
 
   async getChangeRequests() {
     const { data: authData, error: authError } = await supabase.auth.getUser();
     if (authError || !authData.user) {
-      throw new Error('Not authenticated');
+      throw new Error('Authentication required');
     }
 
     const { data: profile, error: profileError } = await supabase
       .from('employee_profiles')
       .select('id')
       .eq('user_id', authData.user.id)
       .single();
 
     if (profileError || !profile) {
-      throw new Error('Profile not found');
+      if (profileError?.code === 'PGRST116') {
+        throw new Error('Employee profile is not linked to this account');
+      }
+      throw new Error('Unable to load profile. Please try again.');
     }
 
     const { data, error } = await supabase
       .from('profile_change_requests')
       .select('*')
       .eq('employee_id', profile.id)
       .order('created_at', { ascending: false });
 
     if (error) {
-      throw error;
+      console.error('Supabase error fetching change requests:', error);
+      throw new Error('Unable to load change requests. Please try again.');
     }
     return data;
   }
